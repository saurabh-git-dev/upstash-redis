{
  "name": "upstash-redis",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc && npx ncc build lib/index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "axios": "^1.6.8"
  }
}
